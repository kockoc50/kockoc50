{
  "name": "claude-for-word",
  "version": "1.0.0",
  "description": "Connect Claude AI to Microsoft Word as an Office Add-in",
  "scripts": {
    "start": "office-addin-debugging start manifest.xml",
    "stop": "office-addin-debugging stop manifest.xml",
    "dev-server": "npx http-server . -p 3000 --ssl --cert certs/localhost.crt --key certs/localhost.key --cors",
    "serve": "npx http-server . -p 3000 --cors",
    "validate": "office-addin-manifest validate manifest.xml",
    "sideload": "office-addin-debugging start manifest.xml desktop"
  },
  "devDependencies": {
    "office-addin-debugging": "^6.0.7",
    "office-addin-dev-certs": "^2.0.7",
    "office-addin-manifest": "^2.1.3",
    "http-server": "^14.1.1"
  },
  "keywords": ["word", "office", "addin", "claude", "anthropic", "ai"],
  "license": "MIT"
}
